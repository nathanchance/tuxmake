[targets]
kernel = zImage

[makevars]
ARCH = hexagon
CROSS_COMPILE = hexagon-linux-musl-
LLVM = 1

[artifacts]
kernel = arch/hexagon/boot/{kernel}

[images]
(clang|llvm).* = tuxmake/clang{version_suffix}
